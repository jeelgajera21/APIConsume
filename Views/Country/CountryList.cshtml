@model IEnumerable<CountryModel>
@using System.Data
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<main id="main" class="main">

    <section class="section">
        <div class="row">
            <div class="col-lg-12">

                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">Country</h5>
                        <div class="row">
                            <div class="text-start col">
                               
                            </div>
                            <div class="text-end col">

                                <a class="text-end " asp-controller="Country" asp-action="CountryAddEdit">
                                    <button class="btn btn-primary mb-3"><i class="bi bi-plus-lg"></i>&nbsp;Add country</button>
                                </a>
                            </div>
                        </div>



                        <!-- End Page Title -->


                        <div class="mb-3">
                            <input type="text" class="form-control" id="citySearch" placeholder="Search Any">
                        </div>

                        <table class="table table-hover table-header-fixed">
                            <thead>
                                <tr>
                                   
                                    <th scope="col">CountryID</th>
                                    <th scope="col">Country Name</th>
                                    <th scope="col">Country Code</th>
                                    <th scope="col">createdDate</th>
                                    <th scope="col">ModifiedDate</th>
                                    <th scope="col" class="text-center">Actions</th>

                                </tr>
                            </thead>
                            <tbody id="cityTable">
                                @foreach (var i in Model)
                                {
                                    <tr>

                                        <td>@i.CountryID</td>
                                        <td>@i.CountryName</td>
                                        <td>@i.CountryCode</td>
                                        <td>@i.CreatedDate</td>
                                        <td>@i.ModifiedDate</td>
                                        <td class="text-center">
                                            <!-- Edit Button -->
                                            <a class="btn btn-outline-success btn-xs me-2" asp-controller="Country" asp-action="CountryAddEdit"
                                               asp-route-CountryID="@i.CountryID">
                                                Edit
                                            </a>
                                            <!-- Delete Button -->
                                            <a class="btn btn-outline-danger btn-xs" asp-controller="Country" asp-action="Delete"
                                               asp-route-CountryID="@i.CountryID"
                                               onclick="return confirm('Are you sure you want to delete this country?');">
                                                Delete
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
</main>
@*
@section Scripts {
    <script>
        $(document).ready(function () {
            $("#citySearch").on("keyup", function () {
                var value = $(this).val().toLowerCase();
                $("#cityTable tr").filter(function () {
                    $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
                });
            });
        });
    </script>
}
 *@